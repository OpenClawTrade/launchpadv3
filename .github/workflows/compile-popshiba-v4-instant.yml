name: Compile PopShiba V4-Instant

on:
  push:
    paths:
      - 'contracts/popshiba/v4-instant/**'
      - '.github/workflows/compile-popshiba-v4-instant.yml'
  workflow_dispatch:

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: nightly

      - name: Init forge libs (if missing)
        run: |
          [ -d lib/forge-std ] || forge install foundry-rs/forge-std --no-commit
          [ -d lib/v4-core ]  || forge install Uniswap/v4-core --no-commit
          [ -d lib/uniswap-hooks ] || forge install OpenZeppelin/uniswap-hooks --no-commit
          [ -d lib/openzeppelin-contracts ] || forge install OpenZeppelin/openzeppelin-contracts --no-commit
          [ -d lib/solmate ] || forge install transmissions11/solmate --no-commit

      - name: Build v4-instant
        run: |
          forge build \
            --root . \
            --contracts contracts/popshiba/v4-instant \
            --out out-v4-instant \
            --via-ir \
            --optimize --optimizer-runs 200 \
            --use 0.8.26 \
            --evm-version cancun

      - name: Extract artifacts
        run: |
          mkdir -p supabase/functions/popv4instant-deploy/artifacts
          for c in PopInstantToken PopInstantHook PopInstantFactory; do
            jq '{abi, bytecode: .bytecode.object, deployedBytecode: .deployedBytecode.object}' \
              "out-v4-instant/${c}.sol/${c}.json" \
              > "supabase/functions/popv4instant-deploy/artifacts/${c}.json"
          done

      - name: Commit artifacts
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add supabase/functions/popv4instant-deploy/artifacts/
          git diff --staged --quiet || git commit -m "ci: rebuild v4-instant artifacts"
          git push || true
