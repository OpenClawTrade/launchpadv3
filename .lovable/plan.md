

# Rename "TUNA OS" to "OpenTuna"

## Change
Update the header brand name from "TUNA OS" to "OpenTuna" in `src/components/layout/AppHeader.tsx`.

## Technical Details

**File**: `src/components/layout/AppHeader.tsx` (line 60)

Change the `<span>` text from `TUNA OS` to `OpenTuna`. Also update the `aria-label` on the link from `"TUNA OS"` to `"OpenTuna"`.

